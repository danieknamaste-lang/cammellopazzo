{
  "name": "cammellopazzo-opzioni-android",
  "version": "1.0.0",
  "description": "App Android dell'Agente Opzioni: parla direttamente col sistema multiagentico sul mac (via Tailscale)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-www.mjs",
    "sync": "node build-www.mjs && cap sync android",
    "apk": "node build-www.mjs && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
