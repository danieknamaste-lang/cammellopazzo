# Dipendenze opzionali
-r requirements.txt

# Backend di trascrizione via API OpenAI (whisper-1 / gpt-4o-transcribe)
openai>=1.60.0

# Separazione delle sorgenti audio (voce / batteria / basso / altro)
demucs>=4.0.1

# Trascrizione musicale audio -> MIDI
basic-pitch>=0.4.0
